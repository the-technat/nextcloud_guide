---
- name: Nextcloud Setup iet-182
  hosts: all # there should be only one server
  become: true
  become_user: root
  vars_files:
  - settings.yml
  roles:
  - role: prep_os
  - role: mariadb
  - role: redis
  - role: php_fpm
  - role: certbot
  - role: nginx
  # - prep_ssmtp
  # - prep_nextcloud
  
